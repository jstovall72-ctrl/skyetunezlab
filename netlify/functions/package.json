{
  "name": "skyetunezlab",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "netlify dev",
    "build": "npm run build --workspace=client",
    "install:all": "npm install && npm install --prefix client"
  },
  "workspaces": ["client"],
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "dependencies": {
    "googleapis": "^144.0.0"
  }
}
